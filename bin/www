#!/usr/bin/env node

const instanceServer = require('../server')

const server = instanceServer.start()

// setTimeout(() => {
//     server.close(() => {
//         console.log('server close return')
//     })
// }, 5000)
